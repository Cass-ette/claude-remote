package dev.clauderemote.android

import android.app.Application

/**
 * Application entry point. Intentionally carries no logic yet; later tasks
 * attach the session repository, OAuth manager, and network stack here.
 */
class ClaudeRemoteApp : Application()
